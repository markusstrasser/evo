#!/usr/bin/env bash
set -euo pipefail

# Activate virtual environment and run material validation
cd "$(dirname "$0")/.."
source .venv/bin/activate
exec python scripts/visual-validate-materials.py "$@"
