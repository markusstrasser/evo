(ns ir.lowering
  "Intent-to-core operation lowering.
   
   This is a stub for converting high-level intents into
   the three core operations. No dependencies on /core.")

;; Placeholder for intent lowering
;; This would convert intents like {:intent :create-text :content "hello"}
;; into core operations like [{:op :create-node ...} {:op :place ...}]