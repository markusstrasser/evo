{:lint-as {clojure.test.check.generators/let        [clojure.core/let]
            clojure.test.check.clojure-test/defspec [clojure.test/deftest]
            promesa.core/let                         [clojure.core/let]}

 :hooks {:analyze-call {kernel.intent/defintent hooks.defintent/defintent}}

 :linters {:syntax                       {:level :off}
           :unresolved-namespace         {:level :error}
           :unresolved-symbol            {:level :error}
           :unused-namespace             {:level         :warning
                                          :exclude       [promesa.core
                                                          clojure.edn
                                                          clojure.string
                                                          lab.anki.fs
                                                          replicant.dom
                                                          ;; Test files with archived/future utilities
                                                          core.permutation-test
                                                          plugins.permute-test
                                                          test-util
                                                          plugins.struct-test
                                                          integration.keybinding-test
                                                          plugins.integration-edge-cases-test
                                                          core.history-test
                                                          golden.runner]}
           :unused-binding               {:level   :warning
                                          ;; Exclude platform-specific and dev files
                                          :exclude [repl            ; REPL helpers use platform-specific bindings
                                                    kernel.dbg      ; Debug helpers
                                                    dev.debug       ; Browser debug
                                                    fixtures        ; Test data generators
                                                    components.block ; Event handlers for side effects
                                                    shell.blocks-ui]}; UI event handlers
           :unused-referred-var          {:level :warning}
           :unused-private-var           {:level :warning}
           :invalid-arity                {:level :error}
           :type-mismatch                {:level :error}
           :misplaced-docstring          {:level   :warning
                                          ;; Exclude test helpers (they use string literals as comments)
                                          :exclude [plugins.selection-test]}
           :missing-docstring            {:level :off}      ; Too noisy for helpers
           :deprecated-var               {:level :warning}
           :redefined-var                {:level :error}
           :refer-all                    {:level :warning}
           :unsorted-required-namespaces {:level :off}
           :unused-import                {:level :warning}
           :shadowed-var                 {:level   :warning
                                          ;; Allow intentional domain names
                                          :exclude [key range next type future
                                                    parents descendants children
                                                    chars ns-name record?
                                                    count db new-db intent result
                                                    children-after new-block-id]}  ; Function calls like (count x) are not shadowing
           :duplicate-require            {:level :error}
           :single-operand-comparison    {:level :warning}
           :single-logical-operand       {:level :warning}
           :redundant-do                 {:level :warning}
           :redundant-let                {:level   :warning
                                          ;; Exclude tests where sequential deps aid clarity
                                          :exclude [core.history-test]}
           :cond-else                    {:level :warning}
           :not-empty?                   {:level :warning}
           :consistent-alias             {:level   :warning
                                          :aliases {medley.core    m
                                                    clojure.set    set
                                                    clojure.string str}}
           ;; Style preferences from STYLE.md
           :datalog-syntax               {:level :off}
           :format                       {:level :off}}  ; Use external formatter

 :dependencies {core.db        #{core.db}
                core.ops       #{core.ops}
                core.interpret #{core.db core.ops core.schema medley.core}
                core.schema    #{}}

 :skip-lint ["test/fixtures"]}
